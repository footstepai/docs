---
title: 'snap_trace'
description: 'Clean up a GPS trace and get road details'
---

Map-match a recorded GPS trace (up to 20,000 points) to the road network. Returns the corrected path with a confidence score, plus road details like surface type, road class, speed limits, and terrain analytics.

Useful for analysing recorded activities (runs, rides, walks) or cleaning noisy GPS data.

## Example prompts

- "Clean up this GPS trace from my bike ride"
- "What roads did I actually travel on during this run?"
- "Snap these GPS coordinates to roads and show me the surface types"
- "Analyse my recorded walk - what was the terrain and road quality like?"

## What you get back

A confidence-scored snapped path, route with terrain analytics (ascent, descent, difficulty), and per-segment road attributes (surface type, road class, speed limit, grade). Also includes per-point match quality so you can see where GPS was noisy.

## Rendering on a map: `include_geometry`

By default this tool returns a lean response — the matched polyline, per-point match details, and sample-by-sample elevation profile are stripped out so standalone LLM agents (which can't render maps) don't pay for them in tokens.

If your client renders results on a map (Mapbox, MapLibre, Leaflet, etc.), pass `include_geometry: true`. The response then carries the matched polyline, per-point match details, and elevation profile alongside the same summary fields. The map renders the geometry; the LLM still reasons over the summary.

Setting `include_geometry: true` on every call from a map-rendering client is tedious. See the [getting-started guide](/mcp/getting-started#map-rendering-clients) for a one-line Vercel AI SDK middleware that injects the flag automatically.
